ocr-scanner 프로젝트에 문서 편집 기능과 언어 안내 문구를 추가해줘.

## 요구사항

### 백엔드
1. DocumentService에 update 메서드 추가:
   - update(Long id, String title, String tags, String ocrText): findById로 문서 찾고, null이 아닌 파라미터만 해당 필드에 set, repository.save 후 반환
2. DocumentController에 PATCH 엔드포인트 추가:
   - @PatchMapping("/{id}") — 요청 body: JSON {title, tags, ocrText} (전부 선택적)
   - @RequestBody로 받아서 service.update(id, title, tags, ocrText) 호출
3. CorsConfig.java의 allowedMethods에 "PATCH" 추가 (현재 GET,POST,DELETE,PUT,OPTIONS만 있음)

### 프론트엔드 (frontend/src/App.jsx)
4. 상세 모달에 편집 기능 추가:
   - 상태 추가: const [editing, setEditing] = useState(false), const [editTitle, setEditTitle] = useState(''), const [editTags, setEditTags] = useState(''), const [editOcr, setEditOcr] = useState('')
   - 모달에 "✏️ 편집" 버튼 추가 → 클릭 시 detail의 값들로 editTitle/editTags/editOcr 채우고 editing=true
   - editing이 true일 때: 제목 input, 태그 input, OCR textarea로 교체 표시 + "💾 저장" 버튼 + "취소" 버튼
   - 저장: fetch `${API}/documents/${detail.id}` PATCH (headers: Content-Type: application/json, body: JSON.stringify({title: editTitle, tags: editTags, ocrText: editOcr})) → 성공 시 editing=false, detail 갱신, loadDocs()
   - 취소: editing=false
5. 언어 안내 문구: 헤더의 서브타이틀 아래에 작은 회색 텍스트로 "🇰🇷 한국어 · 🇺🇸 영어 인식 지원" 추가

### CSS (frontend/src/App.css)
6. 편집 모드 입력/버튼 스타일 추가 (기존 .input, .btn 스타일 톤과 일관되게)

## 주의
- 한국어 주석, 3계층 구조 유지
- 기존 기능(업로드/검색/삭제/목록/OCR) 건드리지 말 것
- 백엔드 mvn compile, 프론트 npm run build 통과 확인
